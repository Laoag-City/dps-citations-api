services:
  dps-citations-api:
    container_name: dps-citations-api
    restart: always
    build: .
    ports:
      - "3002:3002"
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - api-network

networks:
  api-network:
    external: true
    name: api-network
