services:
  dps-citation-api:
    build: .
    ports:
      - "3002:3002"
    #environment:
    #  - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules

#    depends_on:
#      - mongo

#   mongo:
#     image: mongo:latest
#     ports:
#       - "27017:27017"
#     volumes:
#       - mongo-data:/data/db

# volumes:
#   mongo-data:
